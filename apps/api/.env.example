PORT=3000
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=maghami_system
TYPEORM_SYNC=true
CORS_ORIGIN=http://localhost:5173
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=1d
JWT_REFRESH_SECRET=change-me-refresh-in-production
JWT_REFRESH_EXPIRES_IN=7d
# Refresh token is HttpOnly cookie `maghami_system_refresh` (not in JSON body).
# Set COOKIE_SECURE=true behind HTTPS; defaults to secure when NODE_ENV=production.
# COOKIE_SECURE=false
# Cookie Path (default /). Narrow to /v1/auth if you only need refresh/logout.
# REFRESH_COOKIE_PATH=/
BOOTSTRAP_ADMIN_EMAIL=super-admin@localhost.ir
BOOTSTRAP_ADMIN_PASSWORD=UTMwBgi6vcpsXZc
BOOTSTRAP_ADMIN_NAME=super-admin
